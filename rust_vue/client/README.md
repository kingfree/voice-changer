# Vue Client

A minimal Vue 3 interface for testing audio streaming.

Open `index.html` in a modern browser. Press **Connect** to open a WebSocket to the Rust server at `ws://localhost:8080/ws/audio`. Use **Start** and **Stop** to begin and end microphone streaming.
